# M6/M7 (Work Split)

**CSC 667 Term Project**  
**Milestone: M6/M7 (Authentication/Templating)**

Work is divided by layer ownership to enable parallel development.  
Each person owns a set of files and must implement within those boundaries.

👉 After completing **Step 5** in the  
**[M6/M7 Local Setup Guide](local-setup.md)**, continue with the tasks below.

---

## Person 1 — Database (`m6-m7-data`)

### Assigned Files

```text
migrations/<timestamp>_create-users-table.ts
migrations/<timestamp>_create-sessions-table.ts
```

👉 **You are responsible for creating these migration files in the `migrations/` directory.**  
👉 **Do NOT modify existing files outside of this directory.**  
👉 **Do NOT modify locked contracts or files owned by other team members without team agreement.**

### Main Goal

Your job is to complete the **database foundation** for M6/M7.

This includes:

- creating migration files for `users` and `session`
- ensuring migrations match `schema-setup.sql`

## Shared Contracts (Person 1)

The following files are used by the database layer and must remain stable:

- `src/db/users.ts` — baseline database helper functions used by the auth layer
- `src/db/connection.ts` — shared database connection
- `src/types/types.ts` — shared TypeScript interfaces

👉 Do NOT modify these files without team agreement.  
👉 Your responsibility is to ensure your migrations match the schema expected by these files.

### Migration Reference Contracts (Person 1)

The following example files define the expected structure and style of migration files:

- `docs/examples/create-users-table.example.ts`
- `docs/examples/create-sessions-table.example.ts`

👉 Use these as reference templates when writing migrations.  
👉 Do NOT modify these files.  
👉 Your migration files should follow the same structure and conventions.

### Scripts (Person 1)

- `npm run migrate:create -- <name>` — Create a new migration file
- `npm run migrate:up` — Apply all pending migrations to the database
- `npm run migrate:down` — Roll back the most recent migration

- `npm run db:recreate` — Drop and recreate the local database (clean reset)

- `npm run check:db` — Validate database schema and helper functions
- `npm run check:data:pr` — Run full pre-PR validation (lint, format, build, migrate, check)

- `npm run build` — Compile TypeScript
- `npm run lint` — Check for lint errors
- `npm run format:check` — Verify code formatting (Prettier)
- `npm run format` — Automatically format code using Prettier

### Creating Migration Files

Create the following migrations:

```bash
npm run migrate:create -- create-users-table
npm run migrate:create -- create-sessions-table
```

This will generate files in the `migrations/` folder similar to:

```text
migrations/
├─ 1234567891234_create-users-table.ts
└─ 1234567891234_create-sessions-table.ts
```

👉 The numeric prefix is a timestamp automatically generated by `node-pg-migrate`.
👉 Your filenames should match this pattern.

### Schema Reference

The expected database structure and migration examples are defined in:

```text
database/schema-setup.sql
docs/examples/create-users-table.example.ts
docs/examples/create-sessions-table.example.ts
```

These files provide:

- the expected database structure (`schema-setup.sql`)
- examples of how to implement migrations (`create-users-table.example.ts`, `create-sessions-table.example.ts`)
- guidance for `pgm.createTable` usage
- proper `up` and `down` function structure

👉 Your **migration files must match the database structure** defined in **`database/schema-setup.sql` exactly**.
👉 Use the **example files in `docs/examples/*` as the locked contract** when implementing your migration files.
👉 **Do NOT modify the contract** without team agreement.

### What to Run During Development

Use these commands while working:

```bash
npm run build
npm run migrate:up
npm run check:db
```

Use these when needed:

```bash
npm run lint
npm run format:check
npm run format
```

If your database gets into a bad state:

```bash
npm run db:recreate
npm run migrate:up
npm run check:db
```

### Before Opening a PR

Run:

```bash
npm run check:data:pr
```

This runs:

```text
lint
format:check
build
migrate:up
check:db
```

Your branch should not open a PR until this passes locally.

### PR Target

```text
m6-m7-data → m6-m7
```

**👉 Follow the full PR process here:**  
**[Pull Request Guide](../../guides/pull-request-guide.md)**

### Definition of Done

Person 1 is done when:

- both migrations are complete
- migrations match `database/schema-setup.sql`
- database builds cleanly from scratch (`db:recreate → migrate:up`)
- `src/db/users.ts` is implemented
- `npm run check:db` passes
- `npm run check:data:pr` passes
- only assigned files were modified
