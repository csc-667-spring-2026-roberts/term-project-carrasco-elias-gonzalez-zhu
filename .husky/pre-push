# Temporarily disabled for M6/M7 integration
# branch="$(git rev-parse --abbrev-ref HEAD)"
#
# if [ "$branch" = "m6-m7" ]; then
#   echo "🚫 Direct pushes to m6-m7 are not allowed."
#   echo "👉 Create a branch and open a PR instead."
#   exit 1
# fi